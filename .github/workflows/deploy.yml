name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    - name: Cache dependencies
      uses: actions/cache@v3
      with:
        path: frontend/node_modules
        key: ${{ runner.os }}-build-${{ hashFiles('frontend/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-build-

    - name: Install dependencies
      run: |
        cd frontend
        rm -rf node_modules package-lock.json
        npm install --legacy-peer-dep

    - name: Install dependencies
      run: |
        cd frontend
        rm -rf node_modules package-lock.json
        npm ci

    - name: Fix react-scripts permissions
      run: |
        chmod +x node_modules/.bin/react-scripts
      working-directory: frontend

    - name: Uninstall react-script
      run: |
        npm uninstall react-scripts

    - name: Fix react-script to a certain version
      run: |
        npm install react-scripts@5.0.1 --legacy-peer-dep

    - name: Build
      run: |
        cd frontend
        npm run build

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: frontend/build
        # Remove the "cname" line unless you use a custom domain
