    </main>

    <!-- Footer -->
    <footer class="relative overflow-hidden bg-gradient-to-br from-slate-900 via-gray-900 to-blue-950 border-t border-gray-800">
        <!-- Background Elements -->
        <div class="absolute inset-0 overflow-hidden">
            <div class="absolute -top-40 -right-40 w-80 h-80 bg-blue-500/10 rounded-full blur-3xl"></div>
            <div class="absolute -bottom-40 -left-40 w-80 h-80 bg-purple-500/10 rounded-full blur-3xl"></div>
            <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-96 h-96 bg-gradient-radial from-indigo-500/5 to-transparent rounded-full"></div>
        </div>

        <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
            <!-- Main Footer Content -->
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-8">

                <!-- Brand Section -->
                <div class="space-y-6">
                    <div class="flex items-center space-x-3">
                        <div class="w-12 h-12 bg-gradient-to-br from-blue-500 to-purple-600 rounded-2xl flex items-center justify-center shadow-lg">
                            <i class="fas fa-qrcode text-white text-xl"></i>
                        </div>
                        <div>
                            <h3 class="text-xl font-display font-bold text-white">QR Attendance</h3>
                            <p class="text-gray-400 text-sm">Smart School System</p>
                        </div>
                    </div>

                    <p class="text-gray-300 leading-relaxed">
                        Sistem absensi modern dengan teknologi QR Code dan manajemen surat ijin digital yang terintegrasi untuk pendidikan masa depan.
                    </p>

                    <!-- Social Links -->
                    <div class="flex space-x-4">
                        <a href="#" class="w-10 h-10 bg-white/10 hover:bg-blue-500/20 rounded-xl flex items-center justify-center transition-all duration-300 group">
                            <i class="fab fa-facebook-f text-gray-400 group-hover:text-blue-400 transition-colors"></i>
                        </a>
                        <a href="#" class="w-10 h-10 bg-white/10 hover:bg-blue-500/20 rounded-xl flex items-center justify-center transition-all duration-300 group">
                            <i class="fab fa-twitter text-gray-400 group-hover:text-blue-400 transition-colors"></i>
                        </a>
                        <a href="#" class="w-10 h-10 bg-white/10 hover:bg-blue-500/20 rounded-xl flex items-center justify-center transition-all duration-300 group">
                            <i class="fab fa-instagram text-gray-400 group-hover:text-pink-400 transition-colors"></i>
                        </a>
                        <a href="#" class="w-10 h-10 bg-white/10 hover:bg-blue-500/20 rounded-xl flex items-center justify-center transition-all duration-300 group">
                            <i class="fab fa-github text-gray-400 group-hover:text-white transition-colors"></i>
                        </a>
                    </div>
                </div>

                <!-- Quick Links -->
                <div class="space-y-6">
                    <h4 class="text-lg font-semibold text-white">Quick Links</h4>
                    <div class="grid grid-cols-2 gap-3">
                        <a href="#" class="text-gray-400 hover:text-white transition-colors duration-300 flex items-center space-x-2 group">
                            <i class="fas fa-tachometer-alt text-xs group-hover:text-blue-400 transition-colors"></i>
                            <span>Dashboard</span>
                        </a>
                        <a href="#" class="text-gray-400 hover:text-white transition-colors duration-300 flex items-center space-x-2 group">
                            <i class="fas fa-qrcode text-xs group-hover:text-green-400 transition-colors"></i>
                            <span>QR Scanner</span>
                        </a>
                        <a href="#" class="text-gray-400 hover:text-white transition-colors duration-300 flex items-center space-x-2 group">
                            <i class="fas fa-file-medical text-xs group-hover:text-red-400 transition-colors"></i>
                            <span>Surat Ijin</span>
                        </a>
                        <a href="#" class="text-gray-400 hover:text-white transition-colors duration-300 flex items-center space-x-2 group">
                            <i class="fas fa-chart-bar text-xs group-hover:text-purple-400 transition-colors"></i>
                            <span>Laporan</span>
                        </a>
                        <a href="#" class="text-gray-400 hover:text-white transition-colors duration-300 flex items-center space-x-2 group">
                            <i class="fas fa-cog text-xs group-hover:text-yellow-400 transition-colors"></i>
                            <span>Pengaturan</span>
                        </a>
                        <a href="#" class="text-gray-400 hover:text-white transition-colors duration-300 flex items-center space-x-2 group">
                            <i class="fas fa-question-circle text-xs group-hover:text-indigo-400 transition-colors"></i>
                            <span>Bantuan</span>
                        </a>
                    </div>
                </div>

                <!-- Contact & Support -->
                <div class="space-y-6">
                    <h4 class="text-lg font-semibold text-white">Support</h4>
                    <div class="space-y-4">
                        <div class="flex items-start space-x-3">
                            <div class="w-8 h-8 bg-green-500/20 rounded-lg flex items-center justify-center mt-0.5">
                                <i class="fas fa-envelope text-green-400 text-sm"></i>
                            </div>
                            <div>
                                <p class="text-gray-400 text-sm">Email Support</p>
                                <p class="text-white font-medium">support@qrattendance.com</p>
                            </div>
                        </div>

                        <div class="flex items-start space-x-3">
                            <div class="w-8 h-8 bg-blue-500/20 rounded-lg flex items-center justify-center mt-0.5">
                                <i class="fas fa-phone text-blue-400 text-sm"></i>
                            </div>
                            <div>
                                <p class="text-gray-400 text-sm">Phone Support</p>
                                <p class="text-white font-medium">+62 821-1234-5678</p>
                            </div>
                        </div>

                        <div class="flex items-start space-x-3">
                            <div class="w-8 h-8 bg-purple-500/20 rounded-lg flex items-center justify-center mt-0.5">
                                <i class="fas fa-clock text-purple-400 text-sm"></i>
                            </div>
                            <div>
                                <p class="text-gray-400 text-sm">Working Hours</p>
                                <p class="text-white font-medium">24/7 Available</p>
                            </div>
                        </div>
                    </div>

                    <!-- Status Badge -->
                    <div class="inline-flex items-center space-x-2 bg-green-500/10 px-3 py-2 rounded-full border border-green-500/20">
                        <div class="w-2 h-2 bg-green-400 rounded-full animate-pulse"></div>
                        <span class="text-green-400 text-sm font-medium">System Online</span>
                    </div>
                </div>
            </div>

            <!-- Bottom Bar -->
            <div class="border-t border-gray-800 pt-8">
                <div class="flex flex-col md:flex-row items-center justify-between space-y-4 md:space-y-0">
                    <!-- Copyright -->
                    <div class="flex items-center space-x-4 text-center md:text-left">
                        <p class="text-gray-400 text-sm">
                            © <?php echo date('Y'); ?> QR Attendance System. All rights reserved.
                        </p>
                        <div class="hidden md:flex items-center space-x-4 text-gray-500 text-xs">
                            <span>•</span>
                            <a href="#" class="hover:text-white transition-colors">Privacy Policy</a>
                            <span>•</span>
                            <a href="#" class="hover:text-white transition-colors">Terms of Service</a>
                            <span>•</span>
                            <a href="#" class="hover:text-white transition-colors">Security</a>
                        </div>
                    </div>

                    <!-- Tech Stack & Version -->
                    <div class="flex items-center space-x-6">
                        <div class="flex items-center space-x-3">
                            <div class="flex items-center space-x-1">
                                <i class="fab fa-php text-indigo-400 text-lg"></i>
                                <span class="text-gray-400 text-xs">PHP 8.2</span>
                            </div>
                            <div class="flex items-center space-x-1">
                                <i class="fas fa-database text-blue-400 text-sm"></i>
                                <span class="text-gray-400 text-xs">MySQL</span>
                            </div>
                        </div>

                        <div class="flex items-center space-x-2 bg-white/5 px-3 py-1 rounded-full">
                            <div class="w-2 h-2 bg-blue-400 rounded-full animate-pulse"></div>
                            <span class="text-gray-300 text-xs font-medium">v1.0.0</span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Performance Info (Development) -->
            <div class="mt-6 text-center">
                <div class="inline-flex items-center space-x-4 text-xs text-gray-500">
                    <div class="flex items-center space-x-1">
                        <i class="fas fa-clock"></i>
                        <span>Load time: <span id="page-load-time">-</span>ms</span>
                    </div>
                    <span>•</span>
                    <div class="flex items-center space-x-1">
                        <i class="fas fa-memory"></i>
                        <span>Memory: <?php echo function_exists('memory_get_usage') ? round(memory_get_usage(true)/1024/1024, 2) . 'MB' : 'N/A'; ?></span>
                    </div>
                    <span>•</span>
                    <div class="flex items-center space-x-1">
                        <i class="fas fa-server"></i>
                        <span><?php echo $_SERVER['SERVER_SOFTWARE'] ?? 'Unknown Server'; ?></span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Gradient Border Top -->
        <div class="absolute top-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-blue-500/50 to-transparent"></div>
    </footer>

    <!-- Back to Top Button -->
    <button id="back-to-top" class="fixed bottom-8 right-8 w-12 h-12 bg-gradient-to-r from-blue-600 to-purple-600 text-white rounded-2xl shadow-2xl hover:shadow-blue-500/25 transform transition-all duration-300 opacity-0 translate-y-8 pointer-events-none z-50 group">
        <i class="fas fa-arrow-up group-hover:animate-bounce"></i>
    </button>

    <!-- Floating Notification Container -->
    <div id="notification-container" class="fixed top-4 right-4 z-50 space-y-3 max-w-sm"></div>

    <!-- Enhanced Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>

    <script>
        // Enhanced Back to Top functionality
        document.addEventListener('DOMContentLoaded', function() {
            const backToTopBtn = document.getElementById('back-to-top');
            let scrollTimer = null;

            function handleScroll() {
                if (scrollTimer !== null) {
                    clearTimeout(scrollTimer);
                }
                scrollTimer = setTimeout(() => {
                    if (window.pageYOffset > 300) {
                        backToTopBtn.classList.remove('opacity-0', 'translate-y-8', 'pointer-events-none');
                        backToTopBtn.classList.add('opacity-100', 'translate-y-0', 'pointer-events-auto');
                    } else {
                        backToTopBtn.classList.add('opacity-0', 'translate-y-8', 'pointer-events-none');
                        backToTopBtn.classList.remove('opacity-100', 'translate-y-0', 'pointer-events-auto');
                    }
                }, 10);
            }

            window.addEventListener('scroll', handleScroll, { passive: true });

            backToTopBtn.addEventListener('click', function() {
                window.scrollTo({
                    top: 0,
                    behavior: 'smooth'
                });
            });
        });

        // Enhanced Notification System
        function createNotification(type, title, message, duration = 5000) {
            const container = document.getElementById('notification-container');
            const notification = document.createElement('div');

            const typeStyles = {
                'success': {
                    bg: 'bg-gradient-to-r from-green-500 to-emerald-600',
                    icon: 'fas fa-check-circle',
                    border: 'border-green-500/20'
                },
                'error': {
                    bg: 'bg-gradient-to-r from-red-500 to-rose-600',
                    icon: 'fas fa-times-circle',
                    border: 'border-red-500/20'
                },
                'warning': {
                    bg: 'bg-gradient-to-r from-yellow-500 to-orange-600',
                    icon: 'fas fa-exclamation-triangle',
                    border: 'border-yellow-500/20'
                },
                'info': {
                    bg: 'bg-gradient-to-r from-blue-500 to-indigo-600',
                    icon: 'fas fa-info-circle',
                    border: 'border-blue-500/20'
                }
            };

            const style = typeStyles[type] || typeStyles['info'];

            notification.className = `transform transition-all duration-500 translate-x-full opacity-0 ${style.bg} text-white rounded-2xl shadow-2xl p-4 backdrop-blur-lg border ${style.border} max-w-sm`;

            notification.innerHTML = `
                <div class="flex items-start space-x-3">
                    <div class="flex-shrink-0">
                        <i class="${style.icon} text-xl"></i>
                    </div>
                    <div class="flex-1 min-w-0">
                        <h4 class="font-semibold text-sm">${title}</h4>
                        <p class="text-sm opacity-90 mt-1">${message}</p>
                    </div>
                    <button onclick="this.parentElement.parentElement.remove()" class="flex-shrink-0 text-white/70 hover:text-white transition-colors">
                        <i class="fas fa-times text-sm"></i>
                    </button>
                </div>
            `;

            container.appendChild(notification);

            // Animate in
            setTimeout(() => {
                notification.classList.remove('translate-x-full', 'opacity-0');
                notification.classList.add('translate-x-0', 'opacity-100');
            }, 100);

            // Auto remove
            setTimeout(() => {
                notification.classList.add('translate-x-full', 'opacity-0');
                setTimeout(() => {
                    if (notification.parentNode) {
                        notification.remove();
                    }
                }, 500);
            }, duration);

            return notification;
        }

        // Enhanced Global Functions
        window.showNotification = createNotification;

        // Performance Monitoring
        window.addEventListener('load', function() {
            const loadTime = performance.timing.loadEventEnd - performance.timing.navigationStart;
            const loadTimeElement = document.getElementById('page-load-time');
            if (loadTimeElement) {
                loadTimeElement.textContent = loadTime;

                // Color code based on performance
                if (loadTime < 1000) {
                    loadTimeElement.className = 'text-green-400';
                } else if (loadTime < 3000) {
                    loadTimeElement.className = 'text-yellow-400';
                } else {
                    loadTimeElement.className = 'text-red-400';
                }
            }
        });

        // Enhanced Global AJAX Configuration
        if (typeof axios !== 'undefined') {
            axios.defaults.headers.common['X-Requested-With'] = 'XMLHttpRequest';
            axios.defaults.timeout = 10000;

            // Request interceptor
            axios.interceptors.request.use(function(config) {
                // Show loading for non-GET requests
                if (config.method !== 'get') {
                    showLoading();
                }
                return config;
            });

            // Response interceptor
            axios.interceptors.response.use(
                function(response) {
                    hideLoading();
                    return response;
                },
                function(error) {
                    hideLoading();

                    let message = 'Network error occurred';
                    if (error.response) {
                        message = error.response.data.message || `Server error: ${error.response.status}`;
                    } else if (error.request) {
                        message = 'No response from server';
                    }

                    createNotification('error', 'Request Failed', message);
                    return Promise.reject(error);
                }
            );
        }

        // Enhanced Button Loading States
        function setButtonLoading(button, loading = true) {
            if (loading) {
                button.disabled = true;
                button.dataset.originalContent = button.innerHTML;
                button.innerHTML = `
                    <div class="flex items-center justify-center space-x-2">
                        <div class="w-4 h-4 border-2 border-white/30 border-t-white rounded-full animate-spin"></div>
                        <span>Processing...</span>
                    </div>
                `;
                button.classList.add('opacity-75', 'cursor-not-allowed');
            } else {
                button.disabled = false;
                button.innerHTML = button.dataset.originalContent || button.innerHTML;
                button.classList.remove('opacity-75', 'cursor-not-allowed');
            }
        }

        // Global utility functions
        window.setButtonLoading = setButtonLoading;
        window.createNotification = createNotification;

        // Console branding
        console.log(
            '%c🚀 QR Attendance System %cv1.0.0',
            'background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; padding: 8px 16px; border-radius: 8px; font-weight: bold; font-size: 16px;',
            'background: #f1f5f9; color: #64748b; padding: 8px 16px; border-radius: 8px; font-size: 12px; margin-left: 8px;'
        );
        console.log('%cBuilt with ❤️ using Modern Web Technologies', 'color: #64748b; font-size: 12px; font-style: italic;');
    </script>

    <!-- Page-specific scripts placeholder -->
    <?php if (isset($page_scripts) && is_array($page_scripts)): ?>
        <?php foreach ($page_scripts as $script): ?>
            <script src="<?php echo htmlspecialchars($script); ?>"></script>
        <?php endforeach; ?>
    <?php endif; ?>

    <!-- Inline page scripts placeholder -->
    <?php if (isset($inline_scripts)): ?>
        <script><?php echo $inline_scripts; ?></script>
    <?php endif; ?>

</body>
</html>
